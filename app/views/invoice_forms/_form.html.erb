<%= simple_form_for(@invoice_form) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: "Project Name" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
